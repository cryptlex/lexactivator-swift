version: 2
updates:
  # Keeps the actions in .github/workflows current. There are no Swift package
  # dependencies to track — this package deliberately has none.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: "ci"
